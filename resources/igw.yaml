apiVersion: ec2.aws.crossplane.io/v1beta1
kind: InternetGateway
metadata:
  name: internetgateway
spec:
  forProvider:
    region: us-east-1
    vpcIdRef:
      name: playground-vpc
  providerConfigRef:
    name: default
